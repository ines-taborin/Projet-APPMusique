import { ChansonParolesPipe } from './chanson-paroles.pipe';

describe('ChansonParolesPipe', () => {
  it('create an instance', () => {
    const pipe = new ChansonParolesPipe();
    expect(pipe).toBeTruthy();
  });
});

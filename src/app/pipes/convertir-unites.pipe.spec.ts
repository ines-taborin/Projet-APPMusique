import { ConvertirUnitesPipe } from './convertir-unites.pipe';

describe('ConvertirUnitesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirUnitesPipe();
    expect(pipe).toBeTruthy();
  });
});

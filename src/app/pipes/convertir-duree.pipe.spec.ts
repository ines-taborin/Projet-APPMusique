import { ConvertirDureePipe } from './convertir-duree.pipe';

describe('ConvertirDureePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirDureePipe();
    expect(pipe).toBeTruthy();
  });
});

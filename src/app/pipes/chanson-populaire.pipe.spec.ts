import { ChansonPopulairePipe } from './chanson-populaire.pipe';

describe('ChansonPopulairePipe', () => {
  it('create an instance', () => {
    const pipe = new ChansonPopulairePipe();
    expect(pipe).toBeTruthy();
  });
});
